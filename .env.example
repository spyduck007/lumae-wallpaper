# Optional Developer ID signing identity, e.g. Developer ID Application: Example (TEAMID)
DEVELOPER_ID_APPLICATION=
# Keychain profile created with `xcrun notarytool store-credentials`
NOTARY_PROFILE=
# Sparkle Keychain account used for signing update archives
SPARKLE_KEY_ACCOUNT=com.lumae.wallpaper
